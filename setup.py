from setuptools import setup, find_packages

setup(

    name = "chainwalletSdk",

    version = "0.1",

    url = 'https://github.com/1314fire/chainwallet-python-sdk.git',

    long_description = open('README.md').read(),

    packages = find_packages(),

)